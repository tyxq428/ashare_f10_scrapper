name: Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-during-materialization:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic Test is temporarily paused on this closed staging branch; Codex remains disabled."
