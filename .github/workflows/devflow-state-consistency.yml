name: Devflow State Consistency

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-during-materialization:
    runs-on: ubuntu-latest
    steps:
      - run: echo "State checks are temporarily paused on this closed staging branch; Codex remains disabled."
