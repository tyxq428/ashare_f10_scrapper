name: Bootstrap F10 research platform

on:
  workflow_dispatch:
  push:
    branches:
      - feature/f10-research-platform-v1
    paths:
      - ".bootstrap/**"
      - ".github/workflows/bootstrap-project.yml"

permissions:
  contents: write

concurrency:
  group: bootstrap-f10-research-platform-v1
  cancel-in-progress: false

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout bootstrap helper from main
        uses: actions/checkout@v4
        with:
          ref: main
          path: dispatcher
          fetch-depth: 1
          show-progress: false

      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/f10-research-platform-v1
          path: target
          fetch-depth: 0
          show-progress: false

      - name: Recover structurally valid archive and apply validated patch
        shell: bash
        run: bash dispatcher/.github/scripts/bootstrap-diagnostic-wrapper.sh target

      - name: Upload bootstrap diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bootstrap-diagnostic-${{ github.run_id }}
          path: |
            target/bootstrap-diagnostic.log
            target/bootstrap-diagnostic.json
            target/bootstrap-structural-candidates.json
            target/bootstrap-selected-candidate.json
          if-no-files-found: warn
          retention-days: 7

      - name: Commit diagnostics on failure
        if: failure()
        working-directory: target
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f bootstrap-diagnostic.log bootstrap-diagnostic.json
          test ! -f bootstrap-structural-candidates.json || git add -f bootstrap-structural-candidates.json
          test ! -f bootstrap-selected-candidate.json || git add -f bootstrap-selected-candidate.json
          git commit -m "Record bootstrap diagnostic"
          git push origin HEAD:feature/f10-research-platform-v1

      - name: Commit generated project
        if: success()
        working-directory: target
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Nothing to commit"
          else
            git commit -m "Build F10 research platform v1"
            git push origin HEAD:feature/f10-research-platform-v1
          fi
