# W05 Plan：升级兼容与最小 Codex 资格

## 目标

- 验证 State/Descriptor v1 与 v2 的读取和迁移；
- 十次历史额度浪费 Run 必须全部路由到 ChatGPT Web 或确定性修复；
- 将来候选必须绑定用户授权、Descriptor 哈希、失败指纹、复现结果、失败文件范围、Context Budget 和 Usage Ledger；
- 即使资格通过，当前 Workflow 仍不包含模型 Job。
